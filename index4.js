// Écrivez une boucle for qui affiche les nombres de 1 à 10, en utilisant l’opérateur d’incrémentation ++

// initialisation , condition , instruction
for (let index = 1 ; index<10 ; index++){
    console.log(index)
}