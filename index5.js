// Écrivez une boucle while qui affiche les nombres de 10 à 1, en utilisant l’opérateur de décrémentation --.

let index = 11

// // tant que 1 est différent de 11 alors décrémenter 

while (index !=1) {
    index--
    console.log(index);
}